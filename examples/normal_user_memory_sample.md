# User memory

## Communication preferences

- The user prefers concise answers and direct answers.
- The user prefers concise answers with no fluff.
- The user likes friendly tone and plain language.
- The user wants one-box copy paste commands that can be run without editing.
- The user wants sources only when needed and asks to avoid long lists.
- Remember that the user prefers the answer first, followed by short supporting details.

## Computer and tools

- The primary device is a Lenovo ThinkPad X1 Carbon running Linux.
- The laptop hostname is aurora.local and the operating system is Fedora Linux.
- The backup phone is a Google Pixel 8 Pro.
- The user uses VS Code as the code editor and Codex as the AI assistant.
- The work account email address is professor@example.com.
- The main project directory is /home/professor/Projects/aisteno.

## Projects and workflow

- The user works on an AI agent project named Miahou with Autonode as its local execution layer.
- The project repository is /home/professor/Projects/aisteno and the project is currently active.
- The user wants dry-run first for every file operation.
- The user wants backups before edits and confirmation before deleting files.
- The user uses Git checkpoints before large changes.
- The workflow requires review before applying generated changes.
- The assistant should ask before changing configuration and should not overwrite existing output by default.

## Open work

- Open task: add structured normal-person memory packing.
- Open task: test the packer against regular user preference files.
- Next action: measure packed output and verify the reduction target.
- Current status: archive mode is working correctly and pack mode is in development.

# Miahou memory sample

Professor prefers low-token mode and no fluff.
Miahou should retain memory injection budget context.
Autonode will backup, merge, and delete only after yup.

Case No. 24-CV-01842 must remain exact.
Device: Google Pixel 8 Pro
Forensic hash: sha256:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
Evidence path: /cases/24-CV-01842/device backup/image.dd

Professor prefers short direct answers, one-box copy paste commands, and no fluff.
Miahou should ask before changing files, preview changes first, and keep backups before edits.
Autonode is the local execution layer for the Miahou agent project and handles file operations, scan, research, monitor, notify, and system tasks.
The current project status is active and the next action is to test normal memory packing.
The user uses VS Code and Codex, wants Git checkpoints before large changes, and prefers sources only when needed.
